type: http
scheme: basic
description: |
    Once you have a username and password from autoSense to access our Business API, generate a **basic authentication token** 
    and include this in the Authorization header of your requests: `Authorization: Basic <auth_token>`
