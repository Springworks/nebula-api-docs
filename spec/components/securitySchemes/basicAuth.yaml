type: http
scheme: basic
description: |
    Using the baisc authentication protocol you can generate your `auth_token` from *username* and *password*. \
    Include this header in your requests: `Authorization: Basic <auth_token>`
